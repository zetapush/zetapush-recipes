/**
 * Upload an avatar in user profile
 */
macroscript uploadAvatar(
	
) {
	/** New upload request */
	var upload = zpServiceFileSystem.newUploadUrl({
		path: AVATAR_PATH
	});

} broadcast { upload } on channel __selfName