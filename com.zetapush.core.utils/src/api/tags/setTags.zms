/**
 *
 */
macroscript setTags(
	/** Tags id */ @NotNull string id,
	/** tags value*/ @NotNull string[] tags
) {
	/** Store tags */
	const { revision, update, value } = await core_utils__setStoredObject({
		id, value: usr:reduce_array_to_map(tags), table: TABLE_TAGS, zpService: zpServiceGda
	});
	/** Destructure result properties */
	value = usr:reduce_map_to_array(value);
} broadcast new Tags(id, revision, update, value) on channel __selfName
