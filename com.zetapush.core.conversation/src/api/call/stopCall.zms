/**
 * Stop a previously started call with one or several recipients.
 *
 * TODO: call id ?
 * TODO: trigger events to be able to execute code like storing call information into database
 */
macroscript stopCall(
	/** hang up the list of users (list of user keys) */ @NotNull array recipients
) {
} broadcast(recipients) { caller: __userKey } on channel __selfName