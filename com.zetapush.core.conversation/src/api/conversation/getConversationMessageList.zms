macroscript getConversationMessageList(
	@ValidConversationRoom map room,
	map page = { pageSize: 100, pageNumber: 0 }
) {
	var { result: { list } } = call core_conversation__getConversationMessageList({ room, page }) hardFail;
} return {
	room, list, page
} on channel __selfName
