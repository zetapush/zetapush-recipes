/**
 * A call was received by call:startCall.
 * Use this macro to accept or reject the previous call.
 *
 * TODO: call id ?
 * TODO: trigger events to be able to execute code like storing call information into database
 */
macroscript acceptCall(
/** the caller that started the call (user key) */ @NotNull string caller,
/** accept or reject call */ boolean accept = true,
/** accept call with audio */ boolean audio = true,
/** accept call with video */ boolean video = true) {
} broadcast(caller) {accept, audio, video, recipient : __userKey} on channel __selfName